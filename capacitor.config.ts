import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amazoncred.meupreco',
  appName: 'Meu Preço - Amazoncred',
  webDir: 'dist'
};

export default config;
